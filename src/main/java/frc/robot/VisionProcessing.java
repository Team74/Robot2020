package frc.robot;

import java.util.HashMap;

public class VisionProcessing {

    public static HashMap<String, Double> targetPoseRelative = new HashMap<String, Double>();

    /**
     * Takes data from the pixy and data about the target and returns distance and angle to target. 
     * @return
     */
    public static HashMap<String, Double> calculateDistanceAngle(String pixyData, int targetData) {
        return targetPoseRelative;
    }
}